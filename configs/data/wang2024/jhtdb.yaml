_target_: src.data.jhtdb_datamodule.JHTDBDataModule
batch_size: 8 # Needs to be divisible by the number of devices (e.g., if in a distributed setup)
num_workers: 4
pin_memory: False
dataset_config_name: "large_50"
