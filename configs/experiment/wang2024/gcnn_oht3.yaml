# @package _global_

defaults:
  - override /data: wang2024/jhtdb
  - override /model: wang2024/gcnn_oht3
  - override /callbacks: [early_stopping_rmse,model_checkpoint]
  - override /trainer: default


tags: ["wang2024", "superresolution", "large_50"]

seed: 0
trainer:
  max_epochs: 50
  accelerator: auto
  devices: auto
  min_epochs: 20

data:
  batch_size: 8

model:
  net:
    in_channels: 9
    out_channels: 3
    hidden_dim: 8
    maxpool: True
  # default optimizer params
  # default scheduler params

logger:
  wandb:
    tags: ${tags}
    group: "superresolution"
    project: "wang2024"
    entity: "uva-dl2"
  aim:
    experiment: "wang2024/superresolution"